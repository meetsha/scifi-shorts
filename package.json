{
  "name": "scifi-short-stories",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-data.mjs",
    "check:links": "node scripts/validate-data.mjs --links",
    "build": "node scripts/build-site.mjs"
  }
}
